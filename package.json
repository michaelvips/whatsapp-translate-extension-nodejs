{
  "name": "whatsapp-web-translator",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension that translates WhatsApp Web message drafts before sending.",
  "scripts": {
    "build": "esbuild src/background.js --bundle --platform=browser --target=chrome114 --outfile=dist/background.js",
    "preview": "http-server . -p 4173 -a 127.0.0.1"
  },
  "dependencies": {
    "@vitalets/google-translate-api": "^9.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "http-server": "^14.1.1"
  }
}
